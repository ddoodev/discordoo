export enum PresenceActivityTypes {
  // Playing {name}
  GAME,
  // Streaming {details}
  STREAMING,
  // Listening to {name}
  LISTENING,
  // Watching {name}
  WATCHING,
  // {emoji?} {name}
  CUSTOM,
  // Competing in {name}
  COMPETING,
}
