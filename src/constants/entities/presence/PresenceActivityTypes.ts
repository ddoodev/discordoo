export enum PresenceActivityTypes {
    // Playing {name}
    Game = 0,
    // Streaming {details}
    Streaming = 1,
    // Listening to {name}
    Listening = 2,
    // Watching {name}
    Watching = 3,
    // {emoji?} {name}
    Custom = 4,
    // Competing in {name}
    Competing = 5
}
