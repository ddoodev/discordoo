export enum AppCommandTypes {
  /** Slash commands */
  CHAT_INPUT = 1,
  /** A UI-based command that shows up when you right click or tap on a user */
  USER,
  /** A UI-based command that shows up when you right click or tap on a message */
  MESSAGE,
}