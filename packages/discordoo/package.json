{
  "name": "discordoo",
  "version": "0.0.1",
  "description": "Discordoo/discordoo is public facing 'full build' of all Discordoo.",
  "main": "dist/index.js",
  "types": "types/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "npm run compile",
    "clean": "rm -r ./dist && rm - tsconfig.tsbuildinfo",
    "compile": "tsc"
  },
  "dependencies": {
    "@discordoo/cache": "^0.1.0",
    "@discordoo/collection": "^0.1.0",
    "@discordoo/core": "^0.1.0",
    "@discordoo/ws": "^0.1.0"
  }
}
