{
  "name": "@discordoo/collection",
  "version": "0.0.1",
  "description": "Discordoo/collection is custom map wrapper for it to work",
  "main": "dist/index.js",
  "types": "types/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "npm run compile",
    "clean": "rm -r ./dist && rm - tsconfig.tsbuildinfo",
    "compile": "tsc"
  }
}
