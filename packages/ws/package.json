{
  "name": "@discordoo/ws",
  "version": "0.1.0",
  "description": "Discordoo/ws is a module for discordoo to interact with the Discord API web socket.",
  "main": "dist/index.js",
  "types": "types/index.d.ts",
  "license": "MIT",
  "dependencies": {
    "erlpack": "^0.1.3",
    "ws": "^7.4.3",
    "zlib-sync": "^0.1.7"
  },
  "devDependencies": {
    "@types/ws": "^7.4.0",
    "@discordoo/core": "^0.1.0",
    "@discordoo/collection": "^0.1.0",
    "@discordoo/util": "^0.1.0"
  },
  "peerDependencies": {
    "@discordoo/core": "^0.1.0",
    "@discordoo/collection": "^0.1.0",
    "@discordoo/util": "^0.1.0"
  },
  "scripts": {
    "build": "npm run compile",
    "clean": "rm -r ./dist && rm -r tsconfig.tsbuildinfo",
    "compile": "tsc"
  }
}
